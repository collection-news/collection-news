import { theme as defaultTheme } from '@chakra-ui/theme'

const fonts = {
  heading: `PingFang HK, Noto Sans TC, ${defaultTheme.fonts.heading}`,
  body: `PingFang HK, Noto Sans TC, ${defaultTheme.fonts.body}`,
}

export default fonts
